# Level 3: Needle in a Haystack

A large log file is sitting in `/home/player/access.log`. The flag is buried somewhere inside it, on a line containing the word `FLAG`.

**Goal:** Use `grep` to find the line with the flag.
